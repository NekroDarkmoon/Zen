client_id = ''
token = ''
guilds = []
uri = 'postgresql://username:password.@localhost:5432/dbname'
prefix = '~'
activity = 'V4 - Prefix(&)'

# a webhook to a channel for bot stats.
stat_webhook = ('',
                "")

# when you generate your webhook, take the token and ID from the URL like so:
# https://discord.com/api/webhooks/<id>/<token>
